@model IEnumerable<WRA.Models.WallOfShameModel>

@{
    ViewData["Title"] = "WallOfShame";
}

<h1>WallOfShame</h1>
<table class="table">
    <thead class="thead-light containerForm">
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.ActionLink("Details", "WallOfShameUserIndex", new { id=item.UserId})
            </td>
        </tr>
}
    </tbody>
</table>
